API_HOST=0.0.0.0
API_PORT=8000
HTTP_TIMEOUT_SECONDS=20
CORS_ALLOW_ORIGINS=*
# Required only for opt-in zero-tap allotment discovery (device-scoped PAN
# sync). Generate a real 32-byte key per deployment -- never share this
# value or commit it -- with:
#   python -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
PAN_ENCRYPTION_KEY=
